{
	"name": "marko-calendar",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"build": "lasso ./browser.json --main src/client.js --inject-into index.html --config lasso-config.json",
		"watch": "lasso ./browser.json --main src/client.js --inject-into index.html --config lasso-config.json --watch",
		"format": "prettier --write --list-different ."
	},
	"license": "MIT",
	"dependencies": {
		"dayjs": "^1.11.11",
		"lasso": "^2.8.4",
		"lasso-cli": "^2.0.6",
		"lasso-marko": "^2.2.0",
		"marko": "^4.3.1"
	},
	"devDependencies": {
		"prettier": "^3.2.5"
	}
}
